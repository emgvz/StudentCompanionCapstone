spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_companion
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

adzuna:
  app:
    id: ${ADZUNA_APP_ID:test}
    key: ${ADZUNA_APP_KEY:test}